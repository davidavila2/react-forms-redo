import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    background: {
      paper: '#f1f1f1',
    },
    text: {
      primary: '#173A5E',
      secondary: '#46505A',
    },
    action: {
      active: '#001E3C',
    }
  },
});